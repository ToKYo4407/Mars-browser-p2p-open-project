{
  "name": "zawar-ishita-aashish-tests",
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "test": "playwright test",
    "test:signaling": "node --test signaling.test.js",
    "test:all": "node --test signaling.test.js && playwright test"
  },
  "devDependencies": {
    "@playwright/test": "^1.44.0"
  },
  "dependencies": {
    "socket.io-client": "^4.7.2",
    "socket.io": "^4.7.2",
    "express": "^4.18.2"
  }
}
